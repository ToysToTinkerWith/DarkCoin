const path=require('path'),fs=require('fs'),assert=require('assert');const root=path.resolve(__dirname,'..'),out=path.join(root,'output/champion-extras-v59/browser');fs.mkdirSync(out,{recursive:true});
const {chromium}=require(path.join(root,'tmp/playground-qa/node_modules/playwright'));
(async()=>{const browser=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true,args:['--enable-unsafe-swiftshader']});const page=await browser.newPage({viewport:{width:1440,height:1000}}),errors=[];page.on('pageerror',e=>errors.push(e.message));page.on('response',r=>{if(r.status()>=400&&r.url().includes('/arena/playground'))errors.push(r.status()+' '+r.url());});
 await page.goto('http://127.0.0.1:3011/arena/playground/extra-preview.html');const frame=()=>page.evaluate(()=>new Promise(r=>requestAnimationFrame(()=>requestAnimationFrame(r))));const loaded=async()=>{await page.waitForFunction(()=>document.querySelector('#extra').options.length===8&&!document.querySelector('#extra').disabled&&document.querySelector('#status').hidden,null,{timeout:120000});await frame();};await loaded();
 const all=await page.locator('#extra option').evaluateAll(os=>os.map(o=>o.value)),requested=process.argv.slice(2),ids=requested.length?all.filter(i=>requested.includes(i)):all;
 for(const id of ids){await page.selectOption('#extra',id);await loaded();await page.check('#skin');
  for(const v of ['front','quarter','side','back']){await page.click('[data-view="'+v+'"]');await frame();await page.locator('#stage').screenshot({path:path.join(out,id+'-'+v+'.png')});}
  await page.click('[data-view="quarter"]');await page.uncheck('#skin');await frame();await page.locator('#stage').screenshot({path:path.join(out,id+'-mask.png')});await page.click('#isolate');await frame();await page.locator('#stage').screenshot({path:path.join(out,id+'-detail.png')});await page.click('#isolate');await page.check('#walk');await frame();await page.uncheck('#walk');console.log('PASS',id);
 }
 await page.selectOption('#extra','golden_feathers');await loaded();await page.check('#skin');await frame();await page.screenshot({path:path.join(out,'extras-preview.png')});assert.deepEqual(errors,[]);fs.writeFileSync(path.join(out,'validation'+(requested.length?'-'+requested.join('-'):'')+'.json'),JSON.stringify({ids,errors,views:6},null,2));await browser.close();
})().catch(e=>{console.error(e);process.exitCode=1});
